# 执行计划

1. 从已核对的主分支创建独立命名分支，保护原工作副本与 v0.2 升级分支。
2. 提取命名兼容为 v0.1.1；迁移主模块，保留旧模块和命令别名、旧初始化 Skill。同步文档、插件、包元数据、初始化检测、Makefile 与 CI。
3. 保留数据 ID、Schema URN 与 v0.1 格式；验证现有数据、新旧入口、真实 wheel 安装及确定性构建。
4. 将远端仓库更名为 digital-sztu，更新 origin，推送并创建命名 PR。检查服务端 CI 通过后正常合并。
5. 核对服务端名称、main SHA 与入口文件；对干净的常用工作副本仅快进并更新项目内安装。把主分支变更合入 v0.2 升级分支，保留所有研究状态。
