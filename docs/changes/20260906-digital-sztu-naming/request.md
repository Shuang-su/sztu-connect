# 用户请求

本变更承接 Digital SZTU 升级、档案入库与多形式图谱呈现计划中的命名要求：

> 仓库、安装包、插件和主命令统一为 `digital-sztu`，Python 主模块使用 `digital_sztu`。
> 保留旧命令、旧模块启动方式、旧初始化 Skill 和旧数据兼容性；不改变已有稳定 ID，不改写历史记录和第三方存档。

用户此前已要求推送并合并；本次指出：

> 仓库、安装包、插件和主命令统一为 `digital-sztu`似乎仍未完成

因此将命名兼容从完整 v0.2 升级中单独交付，让远端仓库、主分支与实际安装入口先统一。完整普查、正式入库、图谱与 Pages 继续按既定验收推进，不以本次更名代替完成。
