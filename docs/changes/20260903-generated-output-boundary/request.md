# 用户请求

## 推送前审阅

> 我自己放宽了，看看推送吧

审阅时，工作分支为 `codex/readme-archive-onboarding`，用户已经自行修改了知识导出 Schema、构建器、知识导出器、隐私扫描器、记录校验器和对应测试。常规 51 项测试通过，但额外的临时仓库测试确认：`data/generated/knowledge` 或 `data/generated/directories` 指向仓库外时，构建仍会返回成功并向外部目录写入文件。

Agent 因此暂停推送，提议只补生成输出目录的边界检查和回归测试，保持用户放宽的内容规则不变。

## 最终实施与发布授权

> 只补这个输出边界检查和回归测试，然后提交并推送，然后合并

## 本轮范围

- 保留用户现有的六个代码、Schema 和测试文件改动，先形成独立提交。
- 新增实现仅涉及生成输出目录的符号链接边界及其回归测试；不改写用户的 README、其他业务文案、扫描等级、记录准入或导出规则。
- 将当前分支已有的 README 和文档提交一并推送，通过 PR 检查后合并至 `main`。
- 不修改 GitHub About，不创建 Release、部署、网站或新 Issue，不导入真实原件。

此前 README 文案与本地提交的请求、计划和记录保留在相邻的 `20260903-readme-archive-onboarding/` 与 `20260903-user-readme-checkpoint/`，本轮不改写那些历史记录。
