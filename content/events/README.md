# Events

每个正式 Event 只保存一次：

```text
content/events/2024/event-example/
├── event.json
└── index.md        # 可选叙事
```

时间未知时使用 `content/events/undated/`。相关人物、组织、地点、制度和主题通过 Link 指向 Node；不要把 Event 内容复制到这些目录。
